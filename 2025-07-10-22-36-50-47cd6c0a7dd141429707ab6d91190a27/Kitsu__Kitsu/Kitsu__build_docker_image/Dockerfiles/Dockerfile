
# AUTO-GENERATED by Dagster Asset Kitsu__build_docker_image
# http://localhost:3000/asset-groups/Kitsu%2Fbuild_docker_image
# https://hub.docker.com/r/cgwire/cgwire
FROM cgwire/cgwire:latest AS kitsu_build_docker_image
LABEL authors="michimussato@gmail.com"

SHELL ["/bin/bash", "-c"]

ARG DEBIAN_FRONTEND=noninteractive

ENV CONTAINER_TIMEZONE=Europe/Zurich
ENV SET_CONTAINER_TIMEZONE=true

ENV LC_ALL=C.UTF-8
ENV LANG=C.UTF-8

RUN apt-get update && apt-get upgrade -y

RUN apt-get install -y --no-install-recommends 'sudo'
RUN apt-get install -y --no-install-recommends 'htop'
RUN apt-get install -y --no-install-recommends 'curl'
RUN apt-get install -y --no-install-recommends 'ffmpeg'


RUN /opt/zou/env/bin/python -m pip install --root-user-action=ignore 'boto3'


RUN apt-get clean

WORKDIR /etc/postgresql/14/main

COPY ./scripts/postgresql.conf .
RUN chmod 0755 postgresql.conf

WORKDIR /opt/zou

COPY ./scripts/init_db.sh .
RUN chmod 0755 init_db.sh

ENTRYPOINT []
